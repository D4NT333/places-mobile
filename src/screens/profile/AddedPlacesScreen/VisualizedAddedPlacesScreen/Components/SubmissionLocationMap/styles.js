import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 180,
    borderWidth: 1.5,
    borderRadius: 12,
    overflow: "hidden",
  },

  map: {
    width: "100%",
    height: "100%",
  },
});

export default styles;