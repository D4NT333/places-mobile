import { StyleSheet } from "react-native";

export default StyleSheet.create({
  card: {
    flex: 1,
    borderWidth: 2,
    borderColor: "#111",
    borderRadius: 10,
    overflow: "hidden",
    backgroundColor: "#fff",
  },
});