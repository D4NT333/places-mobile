import { StyleSheet } from "react-native";

export default StyleSheet.create({
  card: {
    borderWidth: 1,
    borderColor: "#E7E7E7",
    borderRadius: 14,
    overflow: "hidden",
    backgroundColor: "#FFF",
  },
});
