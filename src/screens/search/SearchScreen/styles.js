import { StyleSheet } from "react-native";

export default StyleSheet.create({
  headerTitle: {
    fontSize: 22,
    fontWeight: "700",
    marginBottom: 12,
  },

  listContent: {
    paddingBottom: 24,
  },

  emptyBox: {
    marginTop: 24,
    padding: 14,
    borderWidth: 1,
    borderRadius: 14,
    borderColor: "#E7E7E7",
    backgroundColor: "#FAFAFA",
  },

  emptyTitle: {
    fontSize: 15,
    fontWeight: "700",
    marginBottom: 6,
  },

  emptyText: {
    fontSize: 13,
    opacity: 0.8,
    lineHeight: 18,
  },
});
